name: echo
description: >-
  Smoke-test playbook: one researcher pass that simply summarizes the request.
  Use to verify the daemon -> engine -> agent -> vault -> reply chain.
needsProjectDir: false
stages:
  - type: single
    id: echo
    role: researcher
    brief: >-
      Do NOT browse the web. Simply restate the task in your own words and
      confirm the pipeline works. Two sentences maximum.
